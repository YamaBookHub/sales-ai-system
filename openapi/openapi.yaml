openapi: 3.0.3
info:
  title: Sales AI System API
  version: 1.0.0
servers:
  - url: http://localhost:3000
security:
  - bearerAuth: []
paths:
  /:
    get:
      summary: Local sales dashboard HTML
      security: []
      responses:
        '200':
          description: Local dashboard
          content:
            text/html:
              schema:
                type: string
  /health:
    get:
      summary: Health check
      security: []
      responses:
        '200': { description: OK }
  /api/companies:
    get:
      summary: List companies
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Company list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompanyListResponse'
    post:
      summary: Create company
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateCompanyRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Company' }
  /api/projects:
    get:
      summary: List crowdfunding projects
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Limit'
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/ProjectStatus' }
      responses:
        '200':
          description: Project list
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProjectListResponse' }
    post:
      summary: Create crowdfunding project
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateProjectRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CrowdfundingProject' }
  /api/projects/import/campfire:
    post:
      summary: Import CAMPFIRE project as company, project and lead
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ImportCampfireProjectRequest' }
      responses:
        '200':
          description: Imported
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ImportCampfireProjectResponse' }
  /api/projects/categories/campfire:
    get:
      summary: List CAMPFIRE category candidates for import search
      responses:
        '200':
          description: CAMPFIRE category candidates
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CampfireCategoryListResponse' }
  /api/projects/search/campfire:
    post:
      summary: Search CAMPFIRE project URL candidates before importing
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SearchCampfireProjectsRequest' }
      responses:
        '200':
          description: CAMPFIRE project candidates
          content:
            application/json:
              schema: { $ref: '#/components/schemas/CampfireSearchResponse' }
  /api/leads:
    get:
      summary: List sales leads
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Limit'
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/LeadStatus' }
        - name: priority
          in: query
          schema: { $ref: '#/components/schemas/LeadPriority' }
      responses:
        '200':
          description: Lead list
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LeadListResponse' }
    post:
      summary: Create lead
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateLeadRequest' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SalesLead' }
  /api/leads/{leadId}:
    get:
      summary: Get sales lead
      parameters:
        - $ref: '#/components/parameters/LeadId'
      responses:
        '200':
          description: Lead detail
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SalesLeadWithRelations' }
    patch:
      summary: Update sales lead management fields
      parameters:
        - $ref: '#/components/parameters/LeadId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateLeadRequest' }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SalesLead' }
  /api/leads/{leadId}/score:
    post:
      summary: Recalculate lead score
      parameters:
        - $ref: '#/components/parameters/LeadId'
      responses:
        '200':
          description: Score result
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LeadScore' }
  /api/mails:
    get:
      summary: List mails
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Limit'
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/EmailStatus' }
      responses:
        '200':
          description: Mail list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items: { type: array, items: { $ref: '#/components/schemas/OutreachEmailWithRelations' } }
                  page: { type: integer }
                  limit: { type: integer }
                  total: { type: integer }
  /api/mails/draft:
    post:
      summary: Create AI assisted mail draft
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateMailDraftRequest' }
      responses:
        '201':
          description: Draft created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}/request-review:
    post:
      summary: Request mail review
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Review requested
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}/request-rereview:
    post:
      summary: Request mail re-review after rejection
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Re-review requested
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}:
    patch:
      summary: Update mail subject and body
      parameters:
        - $ref: '#/components/parameters/EmailId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateMailRequest' }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}/checklist:
    get:
      summary: Get pre-send checklist for mail
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Checklist
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MailChecklistResponse' }
    patch:
      summary: Update pre-send checklist for mail
      parameters:
        - $ref: '#/components/parameters/EmailId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/UpdateMailChecklistRequest' }
      responses:
        '200':
          description: Checklist updated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MailChecklistResponse' }
  /api/mails/{emailId}/approve:
    post:
      summary: Approve mail draft
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Approved
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}/reject:
    post:
      summary: Reject mail draft
      parameters:
        - $ref: '#/components/parameters/EmailId'
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RejectMailRequest' }
      responses:
        '200':
          description: Rejected
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}/queue:
    post:
      summary: Queue approved mail for sending
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Queued
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/{emailId}/retry:
    post:
      summary: Retry failed mail
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Retry queued
  /api/mails/{emailId}/cancel:
    post:
      summary: Cancel mail
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200':
          description: Cancelled
          content:
            application/json:
              schema: { $ref: '#/components/schemas/OutreachEmail' }
  /api/mails/threads/{gmailThreadId}:
    get:
      summary: Get mail thread
      parameters:
        - name: gmailThreadId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Thread
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MailThreadResponse' }
  /t/open/{emailId}.png:
    get:
      summary: Tracking pixel endpoint
      security: []
      parameters:
        - $ref: '#/components/parameters/EmailId'
      responses:
        '200': { description: 1x1 gif }
  /t/click/{token}:
    get:
      summary: Tracking link redirect
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
      responses:
        '302': { description: Redirect }
  /api/ai/leads/{leadId}/generate-mail:
    post:
      summary: Generate mail draft by AI
      parameters:
        - $ref: '#/components/parameters/LeadId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GenerateMailRequest' }
      responses:
        '201':
          description: Generated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AiMailGenerationResponse' }
  /api/ai/leads/{leadId}/email-draft:
    post:
      summary: Generate mail draft by AI
      parameters:
        - $ref: '#/components/parameters/LeadId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/GenerateMailRequest' }
      responses:
        '201':
          description: Generated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AiMailGenerationResponse' }
  /api/ai/leads/{leadId}/generations:
    get:
      summary: List AI generations for lead
      parameters:
        - $ref: '#/components/parameters/LeadId'
      responses:
        '200':
          description: AI generation history
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AiGenerationListResponse' }
  /api/ai/replies/{replyId}/classify:
    post:
      summary: Classify reply by AI
      parameters:
        - name: replyId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Classified
          content:
            application/json:
              schema: { $ref: '#/components/schemas/EmailReply' }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    Page:
      name: page
      in: query
      schema: { type: integer, minimum: 1, default: 1 }
    Limit:
      name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
    LeadId:
      name: leadId
      in: path
      required: true
      schema: { type: string, format: uuid }
    EmailId:
      name: emailId
      in: path
      required: true
      schema: { type: string, format: uuid }
  schemas:
    ProjectStatus:
      type: string
      enum: [discovered, active, ended, suspended, unknown]
    LeadStatus:
      type: string
      enum: [discovered, qualified, drafted, reviewing, approved, queued, contacted, replied, meeting_candidate, rejected, no_response, archived]
    LeadPriority:
      type: string
      enum: [high, medium, low]
    EmailStatus:
      type: string
      enum: [draft, in_review, rejected, approved, queued, sending, sent, failed, cancelled]
    ReplyCategory:
      type: string
      enum: [interested, need_info, meeting_request, not_interested, unsubscribe, auto_reply, complaint, unknown]
    Company:
      type: object
      required: [id, name, isBlocked, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        normalizedName: { type: string, nullable: true }
        websiteUrl: { type: string, nullable: true }
        inquiryUrl: { type: string, nullable: true }
        industry: { type: string, nullable: true }
        memo: { type: string, nullable: true }
        isBlocked: { type: boolean }
        blockedReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateCompanyRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1 }
        websiteUrl: { type: string, nullable: true }
        inquiryUrl: { type: string, nullable: true }
        industry: { type: string, nullable: true }
        memo: { type: string, nullable: true }
    CompanyListResponse:
      type: object
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/Company' }
        page: { type: integer }
        limit: { type: integer }
        total: { type: integer }
    CrowdfundingProject:
      type: object
      properties:
        id: { type: string, format: uuid }
        platformId: { type: string, format: uuid }
        companyId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        url: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        amount: { type: integer }
        supporterCount: { type: integer }
        category: { type: string, nullable: true }
        endDate: { type: string, format: date-time, nullable: true }
    CreateProjectRequest:
      type: object
      required: [platformId, title, url]
      properties:
        platformId: { type: string, format: uuid }
        companyId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        url: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        amount: { type: integer, default: 0 }
        supporterCount: { type: integer, default: 0 }
        category: { type: string, nullable: true }
    ImportCampfireProjectRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          example: https://camp-fire.jp/projects/935270/view
    ImportCampfireProjectResponse:
      type: object
      properties:
        platform:
          type: object
          additionalProperties: true
        company: { $ref: '#/components/schemas/Company' }
        project: { $ref: '#/components/schemas/CrowdfundingProject' }
        lead: { $ref: '#/components/schemas/SalesLead' }
        scraped:
          type: object
          additionalProperties: true
    SearchCampfireProjectsRequest:
      type: object
      properties:
        keyword:
          type: string
          description: 会社名、案件名、URL、商品名の曖昧検索キーワード
        category:
          type: string
          description: CAMPFIREカテゴリURL。旧形式としてカテゴリ名も受け付ける。
        amountMin:
          type: integer
          minimum: 0
        amountMax:
          type: integer
          minimum: 0
        supporterMin:
          type: integer
          minimum: 0
        supporterMax:
          type: integer
          minimum: 0
        status:
          type: string
          enum: [active, endingSoon]
          description: activeは現在公開中、endingSoonは終了間近の候補を返す
    CampfireCategoryListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              label: { type: string }
              value:
                type: string
                description: CAMPFIREカテゴリURL
    CampfireSearchItem:
      type: object
      properties:
        title: { type: string }
        url: { type: string }
        amount: { type: integer }
        supporterCount: { type: integer }
        category: { type: string }
        daysLeft: { type: integer, nullable: true }
        isActive: { type: boolean }
        summary: { type: string }
    CampfireSearchResponse:
      type: object
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/CampfireSearchItem' }
        total: { type: integer }
    ProjectListResponse:
      type: object
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/CrowdfundingProject' } }
        page: { type: integer }
        limit: { type: integer }
        total: { type: integer }
    SalesLead:
      type: object
      properties:
        id: { type: string, format: uuid }
        companyId: { type: string, format: uuid }
        projectId: { type: string, format: uuid, nullable: true }
        status: { $ref: '#/components/schemas/LeadStatus' }
        priority: { $ref: '#/components/schemas/LeadPriority' }
        score: { type: integer }
        reason: { type: string, nullable: true }
        source: { type: string }
        ownerMemo: { type: string, nullable: true }
        contactEmail: { type: string, nullable: true }
        contactFormUrl: { type: string, nullable: true }
        siteMessageUrl: { type: string, nullable: true }
        contactMemo: { type: string, nullable: true }
        sendMethod: { type: string, nullable: true }
        sentAt: { type: string, format: date-time, nullable: true }
        nextFollowUpAt: { type: string, format: date-time, nullable: true }
        brandWebsiteUrl: { type: string, nullable: true }
        instagramUrl: { type: string, nullable: true }
        tiktokUrl: { type: string, nullable: true }
        xUrl: { type: string, nullable: true }
        brandAnalysisMemo: { type: string, nullable: true }
        snsAnalysisMemo: { type: string, nullable: true }
        nextActionAt: { type: string, format: date-time, nullable: true }
    CreateLeadRequest:
      type: object
      required: [companyId]
      properties:
        companyId: { type: string, format: uuid }
        projectId: { type: string, format: uuid, nullable: true }
        source: { type: string, default: manual }
        ownerMemo: { type: string, nullable: true }
    UpdateLeadRequest:
      type: object
      properties:
        status: { $ref: '#/components/schemas/LeadStatus' }
        priority: { $ref: '#/components/schemas/LeadPriority' }
        ownerMemo: { type: string, nullable: true }
        contactEmail: { type: string, nullable: true }
        contactFormUrl: { type: string, nullable: true }
        siteMessageUrl: { type: string, nullable: true }
        contactMemo: { type: string, nullable: true }
        sendMethod: { type: string, nullable: true }
        sentAt: { type: string, format: date-time, nullable: true }
        nextFollowUpAt: { type: string, format: date-time, nullable: true }
        nextActionAt: { type: string, format: date-time, nullable: true }
        brandWebsiteUrl: { type: string, nullable: true }
        instagramUrl: { type: string, nullable: true }
        tiktokUrl: { type: string, nullable: true }
        xUrl: { type: string, nullable: true }
        brandAnalysisMemo: { type: string, nullable: true }
        snsAnalysisMemo: { type: string, nullable: true }
    LeadListResponse:
      type: object
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/SalesLeadWithRelations' } }
        page: { type: integer }
        limit: { type: integer }
        total: { type: integer }
    LeadScore:
      type: object
      properties:
        leadId: { type: string, format: uuid }
        amountScore: { type: integer }
        supporterScore: { type: integer }
        urgencyScore: { type: integer }
        fitScore: { type: integer }
        activityScore: { type: integer }
        totalScore: { type: integer }
        reasonJson: { type: object, nullable: true }
    CreateMailDraftRequest:
      type: object
      required: [leadId, templateKey]
      properties:
        leadId: { type: string, format: uuid }
        templateKey: { type: string }
        manualInstruction: { type: string, nullable: true }
    OutreachEmail:
      type: object
      properties:
        id: { type: string, format: uuid }
        leadId: { type: string, format: uuid, nullable: true }
        companyId: { type: string, format: uuid }
        contactId: { type: string, format: uuid, nullable: true }
        status: { $ref: '#/components/schemas/EmailStatus' }
        templateKey: { type: string, nullable: true }
        subject: { type: string }
        body: { type: string }
        toEmail: { type: string, nullable: true }
        scheduledAt: { type: string, format: date-time, nullable: true }
        sentAt: { type: string, format: date-time, nullable: true }
        retryCount: { type: integer }
    OutreachEmailWithRelations:
      allOf:
        - $ref: '#/components/schemas/OutreachEmail'
        - type: object
          properties:
            company: { $ref: '#/components/schemas/Company' }
            lead:
              allOf:
                - $ref: '#/components/schemas/SalesLead'
                - type: object
                  properties:
                    project: { $ref: '#/components/schemas/CrowdfundingProject' }
    SalesLeadWithRelations:
      allOf:
        - $ref: '#/components/schemas/SalesLead'
        - type: object
          properties:
            company: { $ref: '#/components/schemas/Company' }
            project: { $ref: '#/components/schemas/CrowdfundingProject' }
            scores:
              type: array
              items: { $ref: '#/components/schemas/LeadScore' }
    UpdateMailRequest:
      type: object
      properties:
        subject: { type: string }
        body: { type: string }
    MailChecklistItem:
      type: object
      properties:
        id: { type: string, format: uuid }
        emailId: { type: string, format: uuid }
        key: { type: string }
        label: { type: string }
        checked: { type: boolean }
        checkedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    UpdateMailChecklistItem:
      type: object
      required: [key, label, checked]
      properties:
        key: { type: string }
        label: { type: string }
        checked: { type: boolean }
    UpdateMailChecklistRequest:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/UpdateMailChecklistItem' }
    RejectMailRequest:
      type: object
      properties:
        reason: { type: string }
    MailChecklistResponse:
      type: object
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/MailChecklistItem' }
        complete: { type: boolean }
    GenerateMailRequest:
      type: object
      required: [templateKey]
      properties:
        templateKey: { type: string }
        tone: { type: string, default: low_sales_pressure }
    AiMailGenerationResponse:
      type: object
      properties:
        email: { $ref: '#/components/schemas/OutreachEmail' }
        aiGenerationId: { type: string, format: uuid }
        factsUsed: { type: array, items: { type: string } }
        assumptions: { type: array, items: { type: string } }
        riskFlags: { type: array, items: { type: string } }
    AiGeneration:
      type: object
      properties:
        id: { type: string, format: uuid }
        leadId: { type: string, format: uuid, nullable: true }
        emailId: { type: string, format: uuid, nullable: true }
        type: { type: string }
        provider: { type: string }
        model: { type: string }
        promptVersion: { type: string }
        inputJson: { type: object }
        outputJson: { type: object }
        latencyMs: { type: integer, nullable: true }
        tokenInput: { type: integer, nullable: true }
        tokenOutput: { type: integer, nullable: true }
        costUsd: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        email:
          type: object
          nullable: true
          properties:
            id: { type: string, format: uuid }
            subject: { type: string }
            status: { $ref: '#/components/schemas/EmailStatus' }
            createdAt: { type: string, format: date-time }
    AiGenerationListResponse:
      type: object
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AiGeneration' }
        total: { type: integer }
    EmailReply:
      type: object
      properties:
        id: { type: string, format: uuid }
        emailId: { type: string, format: uuid }
        fromEmail: { type: string, nullable: true }
        bodyText: { type: string, nullable: true }
        category: { $ref: '#/components/schemas/ReplyCategory' }
        confidence: { type: number }
        summary: { type: string, nullable: true }
        nextAction: { type: string, nullable: true }
    MailThreadResponse:
      type: object
      properties:
        gmailThreadId: { type: string }
        emails: { type: array, items: { $ref: '#/components/schemas/OutreachEmail' } }
        replies: { type: array, items: { $ref: '#/components/schemas/EmailReply' } }
    ErrorResponse:
      type: object
      properties:
        code: { type: string }
        message: { type: string }
        requestId: { type: string }
